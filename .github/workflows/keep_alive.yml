name: Keep Workflow Alive

on:
  schedule:
    - cron: "0 0 1 * *"  # Runs once a month
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Keep repo active
        run: echo "Keeping workflows active - $(date)"